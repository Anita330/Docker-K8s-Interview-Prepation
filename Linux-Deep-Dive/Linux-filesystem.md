Linux store data on disk drive using a stracture called a filesystem,consisting of files directory and the information needed to access and locate them.

There are many different type of file system.in Gernel imrpvment have been made to filesystem with new release of operating system and each new filesystem have been given different name.
eg. ext3,ext4,xfs, NTFS,FAT

Pipe
A pipe is used by the shell to connect the output of one command directly to the input of another command

sdv 